

// export const convertCampuses = (data) =>
//   data.map(campus => convertCampus(campus));

// export const convertCampus = (campus) => {

//   campus.imageUrl = `/api/campuses/image/${campus.img}`;

//   return campus;

// };


// export const convertStudents = (data) =>
//   data.map(student => convertStudent(student));

// export const convertStudent = (student) => {

//   student.imageUrl = `/api/students/image${student.img}`;

//   return student;

// };


